export interface Coins {
  image: {
    alt: string;
    asset: {
      _ref: string;
    };
  };
  text: string;
  span: string;
  price: string;
  chartImage: {
    chartAlt: string;
    asset: {
      _ref: string;
    };
  };
}
